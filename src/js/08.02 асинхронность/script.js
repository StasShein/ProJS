//таймеры
setTimeout(() => {}, time)//установить время через которое начнется действие

setInterval(() => {}, interval);//запускает бесконечный цикл повторения действий с определенным интервалом

clearInterval(name||id)//останавливает интервал



?? 
resolve
reject
//then catch
